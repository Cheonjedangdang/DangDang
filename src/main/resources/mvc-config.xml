<?xml version="1.0" encoding="UTF-8"?>
<mvc:interceptors>
        <mvc:interceptor>
           <mvc:mapping path="write"/>
           <!-- 여러개 컨트롤러 추가 가능 -->
           <!--
                <mvc:mapping path="/admin_write"/>
                <mvc:mapping path="/admin_update"/>
                <mvc:mapping path="/admin_delete"/>
                .
                .
                .
           -->
           <!-- 로그인체크 인터셉터 클래스 -->
           <bean class="com.dangdangcompany.dangdang.common.interceptor.Interceptor"/>
        </mvc:interceptor>
</mvc:interceptors>
